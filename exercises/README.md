# Take-home exercise files

This folder contains worksheets (`.pdf`) and the source files to build them from scratch (`.tex` or `.rmd`)

Solutions (`.pdf`) are located in the `solutions/` sub-directory
